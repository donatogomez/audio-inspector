/// Pure DSP and evidence engines. Depends only on the domain (and, later, Accelerate/vDSP).
///
/// Empty at the scaffolding stage; metrics and analyses arrive in their own vertical slices, after
/// the end-to-end flow works (see docs/project-principles.md #13).
public enum AudioInspectorAnalysis {}
