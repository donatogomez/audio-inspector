/// The analysis/detail feature (SwiftUI surface + model). Depends only on the domain; it never
/// imports the media/analysis concretes — the composition root injects what it needs.
///
/// Empty at the scaffolding stage.
public enum FeatureAnalysis {}
