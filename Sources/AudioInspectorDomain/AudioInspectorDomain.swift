/// The pure domain layer: value types, ports (protocols), use cases, and errors.
///
/// This module depends on nothing framework-side (no SwiftUI/AppKit/AVFoundation/AudioToolbox/
/// Accelerate/SwiftData, no `Process`). It is intentionally empty at the scaffolding stage; real
/// domain types arrive with the first vertical slice.
public enum AudioInspectorDomain {}
