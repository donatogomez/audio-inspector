# add-basic-audio-file-inspection

First functional vertical slice (spec only): select a local audio file, open it, read basic technical metadata (no DSP), show a structured result, and export it as schemaVersion 1 JSON.
