# add-waveform-visualization

Read the samples once and draw a static amplitude envelope beside the report: a bounded min/max envelope over all channels, unnormalised, never inside the InspectionReport or the JSON.
