# add-static-spectrogram-visualization

Draw a static, read-only spectrogram of the whole file beside the report: an STFT reduced to a bounded model, on an absolute dBFS scale, linear to Nyquist, never normalised, never inside the InspectionReport or the JSON. It shows where energy stops; it does not say why.
